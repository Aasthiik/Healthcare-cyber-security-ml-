# Reproducible, cross-platform requirements (recommended install via conda)
# If you prefer pip-only, create a Python 3.11 venv and run: `pip install -r requirements.repro.txt`

Flask==2.3.3
pandas>=2.0.3
numpy>=1.24.3
scikit-learn>=1.6.1
joblib>=1.3.2
requests>=2.28
pyshark>=0.4.2  # optional: requires external 'tshark' (Wireshark)

# Optional / dev
pytest>=7.0
